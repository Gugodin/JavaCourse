// Function expression

const sumar = function(param1, param2) {
    console.log(param1+param2)
}

// Practicamente asignar una funcion a una variable
// La diferencia entre la sintaxis normal y esta, es que como es una variable si intentas
// utilizar antes de ser DEFINIDA dara error en cambio con la "Normal" no te da error
// (La normal me refiero a la del numero 13)

sumar(1,2)